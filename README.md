# notas:
primer programa en node
```
     --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de la tabla de multiplicar             [number] [required]
  -l, --list     opcion para listar la tabla en el log[boolean] [default: false]
  -h, --hasta    valor hasta cuando se debe hacer la multiplicacion
                                                          [number] [default: 10]
```